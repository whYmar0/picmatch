# ─── Core framework ──────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
resend==2.30.1
python-dotenv==1.0.1
slowapi==0.1.10            # rate limiting (lib + limits transitive)

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0          # LOCAL DEV — SQLite async driver (no install needed)
asyncpg==0.30.0          # PRODUCTION — uncomment for PostgreSQL on Python 3.12

# ─── Auth & Security ─────────────────────────────────────────────────────────
PyJWT==2.10.1              # JWT — pure Python, no Rust required
bcrypt==4.0.1
passlib[bcrypt]==1.7.4

# ─── Validation ──────────────────────────────────────────────────────────────
pydantic[email]==2.10.3
email-validator==2.2.0

# ─── File handling ───────────────────────────────────────────────────────────
aiofiles==24.1.0
Pillow==11.0.0
cloudinary==1.41.0
